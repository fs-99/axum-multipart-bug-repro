[package]
name = "axum-multipart-bug-repro"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = { version = "0.7.5", features = ["multipart"] }
http-body-util = "0.1.2"
tokio = { version = "1.0", features = ["full"] }
tower-http = { version = "0.5.2", features = ["cors", "fs", "trace"] }
